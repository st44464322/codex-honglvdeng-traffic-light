# Codex 红绿灯制作方案

## 要做什么

做一个 macOS 桌面悬浮红绿灯，用来提示 Codex 当前工作状态。

## 状态规则

- 黄灯常亮：Codex 正在干活，先别催。
- 绿灯常亮：任务完成，可以验收；10 分钟后自动变为空闲。
- 红灯先闪烁 10 秒，之后常亮：等用户回复、确认、授权或补文件。
- 全暗：空闲。

## 提示声规则

- 黄灯：无提示声，只亮灯。
- 绿灯：`Glass`，播放 3 秒。
- 红灯：`Basso`，只提醒 10 秒，之后停止声音和闪烁。
- 隐藏静音按钮：底部左侧小圆点，点击后静音/恢复，设置会持久保存。

## 第一版范围

- 原生 macOS 悬浮窗。
- 可以拖动位置。
- 始终置顶。
- 双击切换状态。
- 右键选择状态。
- 命令行切换状态。
- 脚本可以调用命令改变状态。

## 暂时不做

- 不做网页后台。
- 不自动读取 Codex 内部状态。
- 不控制鼠标键盘。
- 不碰任何平台登录态。
- 不做硬件外设。

## 技术路线

- 悬浮窗：Swift + AppKit。
- 命令控制：Python 写入本地状态文件。
- 状态文件：`runtime/state.json`。

## 后续可以加

- 菜单栏图标。
- 更小尺寸模式。
- 接入常用脚本，让脚本开始自动黄灯、完成自动绿灯、需要确认自动红灯；绿灯 10 分钟后自动空闲。

## 已加

- 开机自启：通过 LaunchAgent 安装，登录 macOS 后自动打开。
